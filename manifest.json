{
  "manifest_version": 3,
  "name": "RTX HDR Image Booster",
  "version": "3.5",
  "description": "Lists images found on any page in a popup, and turns directly-opened images into live canvas-stream video so RTX Video HDR tone-maps them like real video. Optional toggle to auto-convert on every page.",
  "permissions": ["storage"],
  "host_permissions": ["<all_urls>"],
  "action": {
    "default_title": "RTX HDR Image Booster — view detected images",
    "default_popup": "popup.html"
  },
  "background": {
    "service_worker": "background.js"
  },
  "content_scripts": [
    {
      "matches": ["<all_urls>"],
      "js": ["content.js"],
      "run_at": "document_idle",
      "all_frames": true
    }
  ]
}
